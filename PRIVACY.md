# TaskSprite 隐私说明

- 文档状态：1.0.0 正式候选版本
- 更新日期：2026-08-19
- 生效日期：2026-08-19

TaskSprite 是一款本地优先的 Windows 桌面待办应用。本说明描述 TaskSprite 当前桌面版本如何处理用户数据。

## TaskSprite 处理的数据

TaskSprite 会在用户设备本地保存：

- 用户创建的待办事项、日期、完成状态和排序；
- 显示条数、隐藏已完成任务、置顶状态和窗口位置等设置；
- 用户主动选择的桌宠图片副本或其本地引用；
- 为恢复损坏本地数据而保留的本地备份记录。

## 数据存储与传输

- 当前核心待办数据保存在用户自己的 Windows 设备上。
- TaskSprite 当前不要求注册或登录产品账户。
- TaskSprite 当前不包含广告、用户行为遥测或第三方分析 SDK。
- TaskSprite 不会把待办内容或用户选择的图片上传给开发者或第三方服务。
- 通过 TaskSprite 卸载程序卸载时，用户可以选择保留或删除个人信息。选择保留后，重新安装可以继续使用原有待办和设置；选择删除后，卸载程序会清除 TaskSprite 管理的个人数据。直接删除程序文件或卸载过程异常中断时，本地数据可能继续保留。

## 本地诊断日志

- TaskSprite 在本机应用日志目录记录启动、退出、托盘操作、窗口状态、日期刷新以及任务数量汇总，用于排查故障。
- 任务汇总只包含总数、今日、逾期、未来、无日期、完成和删除数量；不记录待办标题、任务 ID、用户图片路径或图片内容。
- 日志按约 `1 MB` 轮换，最多保留 `3` 份，默认不会上传给开发者或第三方。
- 用户只有在主动选择分享日志时才会把文件交给支持人员；分享前应自行复核内容。

## 一次性诊断包

- 用户可以从待办栏的“隐私与诊断”入口主动生成本地 ZIP 诊断包；TaskSprite 不会自动生成、上传或发送该文件。
- 默认诊断包只包含技术日志、应用版本、运行环境和任务数量汇总，不包含待办标题、任务 ID、原始数据文件或用户图片。
- 用户可以在每次导出时临时勾选“包含待办详情”。勾选后，ZIP 会额外包含待办标题、日期和完成状态，可能涉及个人隐私。
- “包含待办详情”默认关闭，关闭导出窗口后不会记住上一次选择；未勾选时，前端不会向诊断导出命令传递待办详情。
- 待办详情文件不包含任务 ID、创建时间、删除记录、原始数据库、自定义图片或完整本机文件路径。
- 诊断包保存在本机应用日志目录的 `diagnostic-exports` 子目录。只有用户主动把 ZIP 发送给其他人后，对方才能看到其中内容。

## 软件更新产生的网络连接

- TaskSprite `1.0.0` 通过 GitHub Releases 提供直装安装包和应用内更新，不使用 Microsoft Store 或 TaskSprite 官网作为本次发布渠道。
- 用户主动点击“检查更新”后，TaskSprite 会连接 `github.com` 和 GitHub 的 Release 资产服务，以获取版本信息、更新说明、安装包和 Tauri updater 签名。
- 更新检查和下载不会上传待办内容、用户图片、设置、诊断日志或应用内操作记录。
- GitHub 仍可能按互联网服务的正常方式接收到 IP 地址、请求时间、User-Agent 和下载资源等基础请求信息；相关处理适用 [GitHub 隐私声明](https://docs.github.com/site-policy/privacy-policies/github-privacy-statement)。

## 用户控制

- 用户可以在应用中创建、编辑、完成和删除自己的待办事项。
- 用户可以恢复默认桌宠图片。
- 用户可以自主决定是否生成诊断包，以及是否在单次导出中包含待办详情。
- 用户可以通过卸载流程选择保留或删除个人信息；保留与删除两种路径均已在 Windows 实机验证。
- 当前版本不提供云端账户，因此不存在由 TaskSprite 服务器持有的账户数据副本。

## 儿童与敏感信息

TaskSprite 不是面向儿童设计的服务，也不要求用户提供身份证件、支付信息、健康信息或其他敏感个人信息。请不要在待办文本或桌宠图片中保存不必要的敏感信息。

## 变更

如果未来增加产品账户、跨设备同步、自动上传崩溃报告或其他联网能力，本说明将在功能启用前更新，并明确数据类型、用途、保存方式和第三方接收方。

## 联系方式

如对本隐私说明、TaskSprite 的本地数据处理方式或个人数据删除有疑问，请联系：`3844524317@qq.com`。

发送支持邮件前，请避免附带不必要的待办正文、自定义图片或其他敏感信息。TaskSprite 不会主动要求用户提供账户密码、支付信息或身份证件。
